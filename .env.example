# Anthropic API Key (server-side default — users can also set their own in Settings)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# fal.ai API Key (for reference only — actual key is stored in localStorage via Settings)
FAL_API_KEY=your_fal_api_key_here
